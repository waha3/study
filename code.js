// 迭代遍历二叉树
function walk(tree) {
    if (tree) {
        const list = [];
        const stack = [];

        stack.push(tree);
        list.push(tree.val)

        while (stack.length) {

        }
    }
    
}