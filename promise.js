'use strict';

const pending = 0;
const fulfilled = 1;
const rejected = 2;
const noop = () => {};


function Promise(fn) {
    if (typeof fn !== 'function')
}


function thenable() {}

Promise.prototype.then = function(onFulfilled, onRejected) {

}
