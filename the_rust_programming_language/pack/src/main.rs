// use std::collections::HashMap;
// use std::fmt::Result;
// use std::io::Result as IOResult;
// use crate::garden::vegetables::Asparagus;

pub mod garden;

fn main() {
  // ! 使用包、Crate 和模块管理不断增长的项目

  // ! 模块小抄
  //   let plant = Asparagus {};
  //   println!("i am growing {:?}", plant);

  // ! 使用 use 关键字将路径引入作用域

  //   let mut map = HashMap::new();
  //   map.insert("name", "waha");

  // ! 将模块拆分成多个文件
}
